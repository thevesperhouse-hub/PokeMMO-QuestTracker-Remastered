<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Semi-transparent Dark Background -->
  <SolidColorBrush x:Key="Background" Color="#CC1A1A1A" />
  <!-- Semi-transparent — matches quest card / overlay buttons -->
  <SolidColorBrush x:Key="ButtonBackground" Color="#661A1A1A" />
  <!-- Text: Off-white for readability -->
  <SolidColorBrush x:Key="ButtonText" Color="#EEEEEE" />
  <!-- Task lists: Very transparent dark grays for floating effect -->
  <SolidColorBrush x:Key="TaskOddBackground" Color="#661A1A1A" />
  <SolidColorBrush x:Key="TaskOddText" Color="#DDDDDD" />
  <SolidColorBrush x:Key="TaskEvenBackground" Color="#55111111" />
  <SolidColorBrush x:Key="TaskEvenText" Color="#DDDDDD" />
  <!-- Checkboxes -->
  <SolidColorBrush x:Key="CheckBoxBorder" Color="#6E6E6E" />
  <SolidColorBrush x:Key="CheckBoxFill" Color="#55E8E8E8" />
  <SolidColorBrush x:Key="CheckBoxMark" Color="#F5F5F5" />

  <!-- Minimal checkbox — outline off, light tile + dark check when on -->
  <Style x:Key="ModernCheck" TargetType="CheckBox">
    <Setter Property="Cursor" Value="Hand"/>
    <Setter Property="Focusable" Value="False"/>
    <Setter Property="Foreground" Value="#CCCCCC"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="CheckBox">
          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <Border x:Name="box"
                    Width="20"
                    Height="20"
                    CornerRadius="5"
                    BorderThickness="1.5"
                    Background="Transparent"
                    BorderBrush="{DynamicResource CheckBoxBorder}">
              <Path x:Name="check"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Data="M2,6 L6.5,10.5 L14,3"
                    Stroke="{DynamicResource CheckBoxMark}"
                    StrokeThickness="2"
                    StrokeStartLineCap="Round"
                    StrokeEndLineCap="Round"
                    StrokeLineJoin="Round"
                    Opacity="0"/>
            </Border>
            <ContentPresenter x:Name="contentPresenter"
                              Margin="10,0,0,0"
                              VerticalAlignment="Center"
                              RecognizesAccessKey="True"
                              Content="{TemplateBinding Content}"
                              ContentTemplate="{TemplateBinding ContentTemplate}"/>
          </StackPanel>
          <ControlTemplate.Triggers>
            <Trigger Property="Content" Value="{x:Null}">
              <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <Trigger Property="Content" Value="">
              <Setter TargetName="contentPresenter" Property="Visibility" Value="Collapsed"/>
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsMouseOver" Value="True"/>
                <Condition Property="IsChecked" Value="False"/>
              </MultiTrigger.Conditions>
              <Setter TargetName="box" Property="BorderBrush" Value="#B0B0B0"/>
            </MultiTrigger>
            <Trigger Property="IsChecked" Value="True">
              <Setter TargetName="box" Property="Background" Value="{DynamicResource CheckBoxFill}"/>
              <Setter TargetName="box" Property="BorderBrush" Value="#88E8E8E8"/>
              <Setter TargetName="check" Property="Opacity" Value="0.95"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="{x:Null}">
              <Setter TargetName="box" Property="Background" Value="#55E8E8E8"/>
              <Setter TargetName="box" Property="BorderBrush" Value="#99E8E8E8"/>
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="IsMouseOver" Value="True"/>
                <Condition Property="IsChecked" Value="True"/>
              </MultiTrigger.Conditions>
              <Setter TargetName="box" Property="Background" Value="#6AE8E8E8"/>
              <Setter TargetName="box" Property="BorderBrush" Value="#A8E8E8E8"/>
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>